import React from "react";
import Modal, { ModalContent } from "../components/modal/Modal";

const Home = () => {
  return (
    <div>
      <ModalContent children="modal content" />
    </div>
  );
};

export default Home;
